---
description: UI design, shadcn patterns, and apps/ui-spec constraint policy
globs: apps/admin/**,apps/merchant/**,apps/store/**,packages/ui/**,docs/design/**,apps/ui-spec/**
alwaysApply: false
---

# UI Design

## UI spec (canonical)

- **Source of truth:** `apps/ui-spec/` — see `.cursor/rules/ui-spec.mdc`
- Read `apps/ui-spec/src/app/page.tsx` for allowed components and variants
- **Do not use Figma** for design or implementation in this repo

## Design system

- Reference: `docs/design/design-system.md`, `.cursor/rules/design-system.mdc`, `apps/ui-spec/src/app/globals.css`
- shadcn/ui primitives: mirror `apps/ui-spec/src/components/ui/` in `packages/ui` or app `components/ui/`
- Extend via composition, not forked copies of shadcn source
- Design tokens via ui-spec CSS variables
- No inline styles; use Tailwind utility classes

## Portal layouts

| App | Layout | Key screens (Phase 1) |
|-----|--------|----------------------|
| `apps/admin` | AdminShell (sidebar + tenant switcher) | Dashboard, Merchants, Merchant detail |
| `apps/merchant` | MerchantShell (sidebar) | Dashboard, CRM, Distributors, Bind |
| `apps/store` | StoreShell (Phase 2) | Catalog, Cart, Bind |

Wireframes: `docs/design/phase-1-admin.md`, `docs/design/phase-1-merchant.md`

## Design doc policy

- Screen specs live in `docs/design/<feature>.md`
- Map each element to a ui-spec component (e.g. `Table`, `Dialog`, `Badge`)
- Token or primitive changes: update `apps/ui-spec` first, then portal code

## Accessibility

- Target WCAG 2.1 AA
- Semantic HTML, keyboard navigation, visible focus states
- 44px touch targets on mobile bind flow

## File layout

```
apps/{admin|merchant|store}/
  app/(feature)/       # Route groups
  components/ui/       # shadcn primitives (or import from @meridian/ui)
  components/          # Feature components
packages/ui/
  components/          # Shared shells, DataTable wrappers
```
